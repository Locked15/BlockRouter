{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "edge",
      "request": "launch",
      "name": "LocalHost (Edge)",
      "url": "https://localhost:5002",
      "webRoot": "${workspaceFolder}"
    },
    {
      "type": "chrome",
      "request": "launch",
      "name": "LocalHost (Chrome)",
      "url": "https://localhost:5002",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
